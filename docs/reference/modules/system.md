# ucon.system

System-level value types: `BaseUnits` (the renamed v1.7 `UnitSystem`), the new `UnitSystem` value type, the per-instance `AlgebraCache`, and the `use` / `active` activation surface.

::: ucon.system
    options:
      show_source: false
      members_order: source
      show_root_heading: false
      heading_level: 2
